// const productsModel = require('../models/productModel');

// const productsExist = async (req, res, next) => {
//   const sales = req.body;
// .0ɸ  const products = await Promise.all(salesPromises);
//   const product = products.some((prod) => prod === undefined);
        
//   if (product) {
//     return res
//       .status(404)
//       .json({ message: 'Product not found' }); 
//   }
//   next();

//   r0eturn next();
// };

// module.exports = productsExist;
